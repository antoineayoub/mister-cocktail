<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.error_notification %>
  <%= f.input :description %>
  <%= f.input :ingredient_id, collection: @ingredients, prompt: "Select a category" %>
  <%= f.button :submit, value: 'Submit' %>
<% end %>
